# Items to Refresh on:

As you work through this exercise, write down anything that you needed to look up or struggled to remember here. It can be just a word or two (e.g. "joining strings"). You can use this as a guide of what to make extra sure you're refreshed on before exams and interviews.

- ArrayLists, initializing lists
- Inserting to the start of a list
- ArrayList vs arrays, initializing arrays
- .length vs .length() (ints vs strings)
- charAt()
- String.join()
- sets, size()
- maps, get() containsKey() iterating over them
- objects - private and public fields, constructors, toString